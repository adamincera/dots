def dict<node, num> relax (node u, dict<node, num> w) {
        for (v in u.oute()) {
	
            if ( w[v] > u.oute()[v]) {
                w[v] = u.oute()[v];
            }
            else {}
        }
        return w;
}
